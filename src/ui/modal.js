// create modal when add task clicked
export default function createModal(clickedElement) {
    const form = document.createElement("form");
    form.classList.add("modal");

    // create form name input
   if (clickedElement.textContent === "Add Task") {
    const taskNameTitle = document.createElement("p");
   } 


    // create form description input

    // create form date input

    // create form priority input with select options

    // create form project input that defaults to inbox but renders available projects

    // assembly
}
